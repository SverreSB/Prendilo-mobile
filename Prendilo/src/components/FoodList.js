import React from 'react';
import { Text, View } from 'react-native';

const FoodList = () => {
    return (
        <View>
            <Text>Food</Text>
        </View>
    );
};

export default FoodList;